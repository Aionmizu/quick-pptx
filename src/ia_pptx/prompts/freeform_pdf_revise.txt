You wrote a single-file HTML deck for WeasyPrint. A visual QA pass rendered each page (slide) to an image and identified rendering bugs (text overflow, overlap, cropped content, illegible contrast, orphan words, empty grid cells, footer collisions, broken page breaks).

Your job: revise the HTML+CSS so every reported bug is fixed, WITHOUT regressing other slides.

REPORTED BUGS (per slide):
{bugs_report}

REVISION RULES
==============
1. Fix every reported bug. Common fixes:
   - Overflow / cropped: reduce font-size, reduce padding, set `overflow: hidden`
     on the immediate container, or restructure the layout (drop a column,
     wrap a long line earlier).
   - Overlap: add explicit `gap` in flex/grid, increase margin-block,
     use `position: relative` + offsets carefully.
   - Orphan word in title: insert `<br>` to break earlier, OR reduce
     font-size, OR widen the textbox by removing surrounding padding.
   - Empty grid cell: drop the cell from the grid template, or merge into
     a neighbor with `grid-column: span 2`.
   - Footer collision: ensure footer is `position: absolute; bottom: 0.4in;`
     and main content has bottom padding ≥ 0.7in.
   - Wide roman numeral: cap the size (max 18rem for "III"), apply negative
     letter-spacing, or wrap in a constrained container with overflow.
   - Page break: each slide section must have `page-break-after: always;`
     except the last one. Ensure no element has `page-break-inside: auto`
     within a slide that would split it.
2. Do NOT change content (titles, body text, palette colors, copy). Only fix
   layout/sizing/spacing.
3. Do NOT add new slides or remove slides.
4. Output the COMPLETE revised HTML file — start with `<!DOCTYPE html>`,
   end with `</html>`. No diff, no snippet, no Markdown fences, no commentary.

ORIGINAL HTML (for context):
{original_script}
