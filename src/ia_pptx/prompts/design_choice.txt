You are designing a presentation deck. Before drafting any slide, you must commit to four independent design choices. The user will see slides built from these choices, so they must be deliberate and varied per deck — not safe defaults.

{slide_rules}

USER PROMPT:
{user_prompt}

OPTIONAL HINTS (may be empty):
- Audience: {audience}
- Style direction: {style_direction}
- Deck length: {length} slides

AVAILABLE STYLES (sampled from the design library):
{styles_list}

INSTRUCTION:
Pick ONE option for each of the four dimensions below. Do not default to the first option in any list. Vary your choices based on the user's prompt and audience.

1. layout_grid — choose one of:
   - single-column (clean linear; good for narrative arcs)
   - two-up (compare/contrast; balanced columns)
   - asymmetric (one bold side block, narrower content on the other)
   - bento (modular tile composition with varying sizes)

2. section_structure — choose one of:
   - linear (no dividers; straight progression)
   - divided (explicit section dividers between groups)
   - summary-led (opening summary then expanded sections)
   - nested (nested sub-sections)

3. hierarchy_pattern — choose one of:
   - type-led (large headlines anchor the eye)
   - number-led (data points dominate)
   - image-led (visuals dominate; minimal text)
   - balanced (even weight)

4. content_density — choose one of:
   - minimal (few words, generous whitespace)
   - medium (moderate detail)
   - dense (information-rich)

5. style_name — choose ONE style by name from the AVAILABLE STYLES list above. Match the style to the prompt's mood and audience.

OUTPUT (strict JSON, no commentary):
{{
  "layout_grid": "<one of the 4 layout_grid values>",
  "section_structure": "<one of the 4 section_structure values>",
  "hierarchy_pattern": "<one of the 4 hierarchy_pattern values>",
  "content_density": "<one of the 3 content_density values>",
  "style_name": "<one style name from AVAILABLE STYLES>",
  "rationale": "<one sentence explaining why these four choices fit this prompt>"
}}
