You are converting a user-supplied plan into slide content. The user already decided what the deck contains — your job is to honor their structure and wording, not re-invent the outline.

{slide_rules}

USER PROMPT (deck topic, for context):
{user_prompt}

USER-SUPPLIED PLAN (Markdown-style bullets — top-level bullet = one slide; sub-bullets = that slide's body):
{user_plan}

LANGUAGE: {language_directive}

STRUCTURAL COMMITMENT (already chosen — do not change):
- Layout grid for slides: {layout_grid}
- Section structure: {section_structure}
- Hierarchy pattern: {hierarchy_pattern}
- Content density: {content_density}
- Visual style: {style_name}

KEY TAKEAWAY (the one thing the audience must remember; may be empty):
{key_takeaway}

INSTRUCTIONS:
1. Each top-level bullet in the plan becomes ONE slide. Preserve the order.
2. The top-level bullet's text becomes (or informs) the slide title. Apply Rule 3:
   if the user wrote a topic label ("Background"), upgrade it to a conclusion sentence.
   If the user already wrote a conclusion sentence, keep it verbatim.
3. Sub-bullets become the slide's body. Polish wording only for slide format
   (concise fragments, not paragraphs) — never invent new content not in the plan.
4. If a slide has no sub-bullets, generate 1–3 short bullets that elaborate the title,
   staying faithful to the plan's overall topic.
5. If KEY TAKEAWAY is non-empty: ensure the deck's closing (or most prominent) slide
   reflects it. Do not invent contradictory takeaways.
6. Cap each slide at 6 elements total (Rule 7). If a sub-bullet section exceeds this,
   combine related bullets — do not drop content silently.
7. is_section_divider: set to true ONLY if the user's plan explicitly used a divider-like
   bullet (e.g., a one-word "Part 1" with no sub-bullets). Otherwise false.
8. ICONS — see the ICON VOCABULARY block below. Add an `"icon"` field per
   slide where a specific icon strengthens the message. Skip the field on
   slides where no icon fits — empty is better than wrong.

{icon_vocab}

OUTPUT (strict JSON, no commentary):
{{
  "slides": [
    {{
      "is_section_divider": false,
      "title": "<slide title — conclusion sentence per Rule 3>",
      "body": ["<bullet 1>", "<bullet 2>", ...],
      "icon": "FaLightbulb"
    }},
    ...
  ]
}}

Produce exactly one slide per top-level bullet in the user's plan. The output array length
must match the count of top-level bullets in the plan.
