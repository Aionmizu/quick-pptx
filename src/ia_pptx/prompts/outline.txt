You are drafting the content for a presentation deck.

{slide_rules}

USER PROMPT:
{user_prompt}

LANGUAGE: {language_directive}

STRUCTURAL COMMITMENT (already chosen — do not change):
- Layout grid for slides: {layout_grid}
- Section structure: {section_structure}
- Hierarchy pattern: {hierarchy_pattern}
- Content density: {content_density}
- Visual style: {style_name}

DECK LENGTH: {length} slides total.

KEY TAKEAWAY (the one thing the audience must remember; may be empty):
{key_takeaway}

INSTRUCTIONS:
1. Build a coherent narrative arc across the requested number of slides.
2. Respect the section_structure choice:
   - linear: no section dividers
   - divided: insert section-divider slides between groups (count toward total)
   - summary-led: open with a summary slide
   - nested: produce hierarchical sub-sections
3. Respect the content_density:
   - minimal: 1–2 short bullets per slide; very few words
   - medium: 3–5 bullets per slide; concise phrasings
   - dense: 4–7 bullets per slide; full ideas
4. Respect the hierarchy_pattern in titling:
   - type-led: bold, declarative titles
   - number-led: titles that lead with a key number where natural
   - image-led: short titles; bullets describe the visual concept
   - balanced: mid-weight titles
5. Body lines are bullet-friendly fragments — not multi-sentence paragraphs unless density is "dense".
6. Apply Rule 3 from the design rules: every slide title states the slide's CONCLUSION,
   not its TOPIC. "False-positive rates depend on sample size" beats "Results".
7. Apply Rule 8 (the distracted-person test): a reader who only sees the title and
   skim the bullets must grasp the slide's main point in 2 seconds.
8. Cap total elements per slide at 6 (Rule 7). A title counts as 1 element; each
   bullet is 1 element. So a "dense" slide is the title + at most ~5 bullets.
9. If KEY TAKEAWAY is non-empty: shape the deck so the closing slide (or the most
   prominent one) carries this takeaway in its title or first bullet. Do NOT
   introduce contradictory messages.
10. is_section_divider: set to TRUE only for slides whose ENTIRE purpose is to
    announce a new section — meaning they have NO meaningful content body,
    just a section name and maybe a one-line subtitle. A content slide whose
    title introduces or names a section (e.g., "Roots of unrest") but also
    has bullets is NOT a section divider — its is_section_divider must be FALSE.
    Section dividers should be a small minority of slides (0–2 in a 10-slide deck).
11. Section divider slides should have an empty `body` array or contain at most
    ONE short subtitle line — never multiple bullets.
12. ICONS — see the ICON VOCABULARY block below. Add an `"icon"` field per
    slide where a specific icon strengthens the message (top-right anchor).
    Skip the field on slides where no icon fits — never invent or reuse the
    same icon across many slides.

{icon_vocab}

OUTPUT (strict JSON, no commentary):
{{
  "slides": [
    {{
      "is_section_divider": false,
      "title": "<slide title>",
      "body": ["<bullet 1>", "<bullet 2>", ...],
      "icon": "FaCheckCircle"
    }},
    ...
  ]
}}

The output array must contain exactly {length} entries.
